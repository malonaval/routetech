# Auth + Base de Datos Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir login con Google (Supabase Auth), persistencia de API keys y config de vehículo por usuario, y historial de rutas con cifrado AES-256-GCM de todos los campos PII (nombre, dirección, teléfono de cliente).

**Architecture:** Supabase gestiona Auth, PostgreSQL y Edge Functions. El navegador llama directamente a Supabase Auth. Los campos PII pasan por Edge Functions Deno que cifran/descifran con `ENCRYPTION_KEY` (variable de entorno del servidor, nunca expuesta al cliente). `react-router-dom` añade la ruta `/historial`. Los componentes existentes no se modifican.

**Tech Stack:** React 18 + Vite, @supabase/supabase-js v2, react-router-dom v6, Supabase Edge Functions (Deno), AES-256-GCM (Web Crypto API)

**Spec:** `docs/superpowers/specs/2026-05-27-auth-db-design.md`

---

## File map

| Archivo | Acción | Responsabilidad |
|---|---|---|
| `src/utils/supabaseClient.js` | Crear | Singleton del cliente Supabase |
| `src/pages/LoginPage.jsx` | Crear | Pantalla de login con botón Google |
| `src/pages/HistoryPage.jsx` | Crear | Lista de rutas guardadas + detalle |
| `src/main.jsx` | Modificar | BrowserRouter + rutas + auth guard |
| `src/App.jsx` | Modificar | Cargar settings al inicio, guardar ruta tras optimizar, botón logout |
| `supabase/functions/_shared/crypto.ts` | Crear | Helpers AES-256-GCM reutilizables |
| `supabase/functions/_shared/cors.ts` | Crear | Headers CORS para Edge Functions |
| `supabase/functions/save-settings/index.ts` | Crear | Cifra y guarda API keys + vehículo |
| `supabase/functions/get-settings/index.ts` | Crear | Lee y descifra settings del usuario |
| `supabase/functions/save-route/index.ts` | Crear | Cifra PII y guarda ruta + paradas |
| `supabase/functions/get-routes/index.ts` | Crear | Lee y descifra historial de rutas |
| `.env` | Crear | VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY |
| `.gitignore` | Modificar | Añadir .env |

---

## Task 1: Supabase project + DB schema + Google OAuth

> Pasos en el dashboard de Supabase — sin código JS todavía.

**Files:** ninguno (solo dashboard + SQL)

- [ ] **Step 1: Crear proyecto en Supabase**

  Ir a https://supabase.com → New project → elegir nombre `routetech`, región Europe (Frankfurt), contraseña segura.

  Esperar ~2 min a que el proyecto arranque.

- [ ] **Step 2: Copiar credenciales**

  En el dashboard: Settings → API.
  Copiar:
  - `Project URL` → será `VITE_SUPABASE_URL`
  - `anon public key` → será `VITE_SUPABASE_ANON_KEY`

- [ ] **Step 3: Crear tablas y RLS**

  En el dashboard: SQL Editor → New query. Pegar y ejecutar:

  ```sql
  -- user_settings: configuración por usuario
  CREATE TABLE user_settings (
    user_id         UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    groq_api_key    TEXT,
    google_maps_key TEXT,
    consumption     NUMERIC(5,2),
    fuel_price      NUMERIC(5,3),
    updated_at      TIMESTAMPTZ DEFAULT now()
  );

  -- routes: una fila por optimización
  CREATE TABLE routes (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id        UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    created_at     TIMESTAMPTZ DEFAULT now(),
    origin         TEXT,
    total_km       NUMERIC(6,1),
    saving_minutes INTEGER,
    end_time       TEXT,
    worker_count   INTEGER DEFAULT 1
  );

  -- route_stops: paradas de cada ruta
  CREATE TABLE route_stops (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    route_id       UUID NOT NULL REFERENCES routes(id) ON DELETE CASCADE,
    position       INTEGER NOT NULL,
    client_name    TEXT,
    client_address TEXT,
    client_phone   TEXT,
    duration_min   INTEGER,
    window_type    TEXT,
    window_start   TEXT,
    window_end     TEXT,
    arrival_time   TEXT,
    travel_minutes INTEGER
  );

  -- RLS
  ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;
  ALTER TABLE routes         ENABLE ROW LEVEL SECURITY;
  ALTER TABLE route_stops    ENABLE ROW LEVEL SECURITY;

  CREATE POLICY "own settings" ON user_settings USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
  CREATE POLICY "own routes"   ON routes         USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
  CREATE POLICY "own stops"    ON route_stops    USING (
    route_id IN (SELECT id FROM routes WHERE user_id = auth.uid())
  );
  ```

  Verificar: en Table Editor deben aparecer las 3 tablas.

- [ ] **Step 4: Habilitar Google OAuth**

  Dashboard → Authentication → Providers → Google → Enable.

  Necesitas un Google Cloud project con OAuth 2.0:
  1. https://console.cloud.google.com → APIs & Services → Credentials → Create OAuth 2.0 Client ID
  2. Application type: Web application
  3. Authorized redirect URIs: `https://TU_PROJECT_REF.supabase.co/auth/v1/callback`
     (el `PROJECT_REF` está en Supabase Settings → General)
  4. Copiar Client ID y Client Secret → pegarlos en Supabase → Google provider → Save.

- [ ] **Step 5: Añadir redirect URL en Supabase**

  Dashboard → Authentication → URL Configuration:
  - Site URL: `http://localhost:5173` (dev) — cambiar por la URL de producción cuando se despliegue
  - Redirect URLs: añadir `http://localhost:5173`

---

## Task 2: Dependencias + cliente Supabase + variables de entorno

**Files:**
- Crear: `.env`
- Modificar: `package.json` (vía npm install)
- Crear: `src/utils/supabaseClient.js`
- Modificar: `.gitignore`

- [ ] **Step 1: Instalar dependencias**

  ```bash
  cd C:\Dev\routetech
  npm install @supabase/supabase-js react-router-dom
  ```

  Verificar: aparecen en `package.json` bajo `dependencies`.

- [ ] **Step 2: Crear .env**

  Crear `C:\Dev\routetech\.env`:

  ```
  VITE_SUPABASE_URL=https://TU_PROJECT_REF.supabase.co
  VITE_SUPABASE_ANON_KEY=eyJ...tu_anon_key...
  ```

  Reemplazar con los valores copiados en Task 1 Step 2.

- [ ] **Step 3: Añadir .env a .gitignore**

  Abrir `.gitignore` (o crearlo si no existe) y añadir al final:

  ```
  .env
  .env.local
  ```

- [ ] **Step 4: Crear src/utils/supabaseClient.js**

  ```js
  import { createClient } from '@supabase/supabase-js'

  export const supabase = createClient(
    import.meta.env.VITE_SUPABASE_URL,
    import.meta.env.VITE_SUPABASE_ANON_KEY
  )
  ```

- [ ] **Step 5: Verificar**

  ```bash
  npm run dev
  ```

  La app debe arrancar sin errores. En la consola del navegador:
  ```js
  // Pegar en DevTools console:
  const { supabase } = await import('/src/utils/supabaseClient.js')
  const { data } = await supabase.auth.getSession()
  console.log(data) // { session: null }
  ```
  Debe imprimir `{ session: null }` sin errores de red.

- [ ] **Step 6: Commit**

  ```bash
  git add package.json package-lock.json src/utils/supabaseClient.js .gitignore
  git commit -m "feat: añadir @supabase/supabase-js y react-router-dom, crear supabaseClient"
  ```

---

## Task 3: LoginPage + routing en main.jsx

**Files:**
- Crear: `src/pages/LoginPage.jsx`
- Modificar: `src/main.jsx`

- [ ] **Step 1: Crear src/pages/LoginPage.jsx**

  ```jsx
  import { supabase } from '../utils/supabaseClient'

  export default function LoginPage() {
    const handleGoogleLogin = async () => {
      await supabase.auth.signInWithOAuth({
        provider: 'google',
        options: { redirectTo: window.location.origin },
      })
    }

    return (
      <div className="login-page">
        <div className="login-card">
          <div className="login-logo">RouteTech</div>
          <div className="login-subtitle">Optimización de rutas · IA + Tráfico real</div>
          <button className="login-google-btn" onClick={handleGoogleLogin}>
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
              <path d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844c-.209 1.125-.843 2.078-1.796 2.717v2.258h2.908c1.702-1.567 2.684-3.874 2.684-6.615z" fill="#4285F4"/>
              <path d="M9 18c2.43 0 4.467-.806 5.956-2.18l-2.908-2.259c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 009 18z" fill="#34A853"/>
              <path d="M3.964 10.71A5.41 5.41 0 013.682 9c0-.593.102-1.17.282-1.71V4.958H.957A8.996 8.996 0 000 9c0 1.452.348 2.827.957 4.042l3.007-2.332z" fill="#FBBC05"/>
              <path d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 00.957 4.958L3.964 7.29C4.672 5.163 6.656 3.58 9 3.58z" fill="#EA4335"/>
            </svg>
            Continuar con Google
          </button>
        </div>
      </div>
    )
  }
  ```

- [ ] **Step 2: Añadir CSS para login en src/index.css**

  Añadir al final de `src/index.css`:

  ```css
  /* ── Login page ── */
  .login-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg);
  }
  .login-card {
    background: white;
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 48px 40px;
    text-align: center;
    width: 340px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.06);
  }
  .login-logo {
    font-size: 28px;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -1px;
    margin-bottom: 8px;
  }
  .login-subtitle {
    font-size: 12px;
    color: var(--muted2);
    margin-bottom: 32px;
    letter-spacing: 0.3px;
  }
  .login-google-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    padding: 12px 20px;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: white;
    font-size: 14px;
    font-weight: 500;
    color: var(--text);
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
  }
  .login-google-btn:hover {
    background: var(--bg);
    border-color: var(--muted2);
  }
  .auth-loading {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    color: var(--muted2);
  }
  ```

- [ ] **Step 3: Reescribir src/main.jsx con routing**

  ```jsx
  import { StrictMode, useState, useEffect } from 'react'
  import { createRoot } from 'react-dom/client'
  import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
  import 'leaflet/dist/leaflet.css'
  import './index.css'
  import { supabase } from './utils/supabaseClient'
  import App from './App.jsx'
  import LoginPage from './pages/LoginPage.jsx'
  import HistoryPage from './pages/HistoryPage.jsx'

  function AppRouter() {
    const [session, setSession] = useState(undefined) // undefined = cargando

    useEffect(() => {
      supabase.auth.getSession().then(({ data }) => setSession(data.session))
      const { data: { subscription } } = supabase.auth.onAuthStateChange((_e, s) => setSession(s))
      return () => subscription.unsubscribe()
    }, [])

    if (session === undefined) return <div className="auth-loading">Cargando…</div>

    return (
      <Routes>
        <Route path="/login"     element={session ? <Navigate to="/" replace /> : <LoginPage />} />
        <Route path="/historial" element={session ? <HistoryPage /> : <Navigate to="/login" replace />} />
        <Route path="/*"         element={session ? <App /> : <Navigate to="/login" replace />} />
      </Routes>
    )
  }

  createRoot(document.getElementById('root')).render(
    <StrictMode>
      <BrowserRouter>
        <AppRouter />
      </BrowserRouter>
    </StrictMode>
  )
  ```

- [ ] **Step 4: Crear src/pages/HistoryPage.jsx stub temporal**

  (El componente real se implementa en Task 9; este stub evita errores de import)

  ```jsx
  export default function HistoryPage() {
    return <div style={{ padding: 40, fontFamily: 'Inter, sans-serif' }}>Historial — próximamente</div>
  }
  ```

- [ ] **Step 5: Verificar**

  ```bash
  npm run dev
  ```

  - Abrir http://localhost:5173 → debe redirigir a `/login` y mostrar el botón de Google
  - Abrir http://localhost:5173/historial → también redirige a `/login`
  - La app no debe mostrar errores en consola

- [ ] **Step 6: Commit**

  ```bash
  git add src/main.jsx src/pages/LoginPage.jsx src/pages/HistoryPage.jsx src/index.css
  git commit -m "feat: routing con react-router-dom, LoginPage con Google OAuth"
  ```

---

## Task 4: Botón logout en App.jsx

**Files:**
- Modificar: `src/App.jsx` (solo el header — añadir botón logout)

- [ ] **Step 1: Importar supabase y useNavigate en App.jsx**

  En `src/App.jsx`, añadir a las importaciones existentes:

  ```js
  import { useNavigate } from 'react-router-dom'
  import { supabase } from './utils/supabaseClient'
  ```

- [ ] **Step 2: Añadir handler logout y botón en el header**

  Dentro del componente `App()`, justo después de las declaraciones de estado existentes, añadir:

  ```js
  const navigate = useNavigate()

  const handleLogout = async () => {
    await supabase.auth.signOut()
    navigate('/login')
  }
  ```

  En el JSX del header, localizar `<div className="header-badge">` y añadir el botón justo antes del cierre del `header`:

  ```jsx
  <button
    className="btn-logout"
    onClick={handleLogout}
    title="Cerrar sesión"
  >
    Salir
  </button>
  ```

- [ ] **Step 3: Añadir CSS del botón logout en src/index.css**

  ```css
  .btn-logout {
    font-size: 11px;
    color: var(--muted2);
    background: none;
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 4px 10px;
    cursor: pointer;
    transition: color 0.15s;
  }
  .btn-logout:hover { color: var(--text); }
  ```

- [ ] **Step 4: Verificar**

  - Login con Google → llega a la app principal
  - Hacer clic en "Salir" → redirige a `/login`
  - Volver a `/` → redirige a `/login` (sesión cerrada)

- [ ] **Step 5: Commit**

  ```bash
  git add src/App.jsx src/index.css
  git commit -m "feat: botón logout en header"
  ```

---

## Task 5: Supabase CLI + Edge Functions infrastructure

**Files:**
- Crear: `supabase/functions/_shared/cors.ts`
- Crear: `supabase/functions/_shared/crypto.ts`

- [ ] **Step 1: Instalar Supabase CLI**

  ```bash
  npm install -g supabase
  supabase --version
  ```

  Debe imprimir algo como `1.x.x`.

- [ ] **Step 2: Login y link al proyecto**

  ```bash
  supabase login
  supabase init
  supabase link --project-ref TU_PROJECT_REF
  ```

  El `PROJECT_REF` se encuentra en Supabase dashboard → Settings → General → Reference ID.

- [ ] **Step 3: Crear supabase/functions/_shared/cors.ts**

  ```ts
  export const corsHeaders = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
  }
  ```

- [ ] **Step 4: Crear supabase/functions/_shared/crypto.ts**

  ```ts
  function hexToBytes(hex: string): Uint8Array {
    const bytes = new Uint8Array(hex.length / 2)
    for (let i = 0; i < bytes.length; i++) {
      bytes[i] = parseInt(hex.slice(i * 2, i * 2 + 2), 16)
    }
    return bytes
  }

  async function getKey(mode: 'encrypt' | 'decrypt'): Promise<CryptoKey> {
    const raw = hexToBytes(Deno.env.get('ENCRYPTION_KEY') ?? '')
    return crypto.subtle.importKey('raw', raw, { name: 'AES-GCM' }, false, [mode])
  }

  export async function encrypt(plaintext: string): Promise<string> {
    const key = await getKey('encrypt')
    const iv = crypto.getRandomValues(new Uint8Array(12))
    const ciphertext = await crypto.subtle.encrypt(
      { name: 'AES-GCM', iv },
      key,
      new TextEncoder().encode(plaintext)
    )
    const combined = new Uint8Array(12 + ciphertext.byteLength)
    combined.set(iv, 0)
    combined.set(new Uint8Array(ciphertext), 12)
    return btoa(String.fromCharCode(...combined))
  }

  export async function decrypt(encoded: string): Promise<string> {
    const key = await getKey('decrypt')
    const combined = Uint8Array.from(atob(encoded), c => c.charCodeAt(0))
    const iv = combined.slice(0, 12)
    const ciphertext = combined.slice(12)
    const plaintext = await crypto.subtle.decrypt({ name: 'AES-GCM', iv }, key, ciphertext)
    return new TextDecoder().decode(plaintext)
  }

  // Cifra solo si el valor no es null/undefined/vacío
  export async function encryptIfPresent(v: string | null | undefined): Promise<string | null> {
    if (!v) return null
    return encrypt(v)
  }

  // Descifra solo si el valor existe
  export async function decryptIfPresent(v: string | null | undefined): Promise<string | null> {
    if (!v) return null
    return decrypt(v)
  }
  ```

- [ ] **Step 5: Generar ENCRYPTION_KEY**

  Ejecutar en terminal para generar una clave aleatoria de 32 bytes:

  ```bash
  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
  ```

  Copiar el resultado (64 caracteres hex). Guardar en un lugar seguro — si se pierde, los datos cifrados ya existentes no se pueden descifrar.

- [ ] **Step 6: Establecer el secreto en Supabase**

  ```bash
  supabase secrets set ENCRYPTION_KEY=TU_CLAVE_HEX_DE_64_CHARS
  ```

  Verificar:
  ```bash
  supabase secrets list
  ```
  Debe aparecer `ENCRYPTION_KEY` en la lista.

- [ ] **Step 7: Commit**

  ```bash
  git add supabase/
  git commit -m "feat: Edge Functions scaffold — cors y crypto compartidos"
  ```

---

## Task 6: Edge Functions save-settings + get-settings

**Files:**
- Crear: `supabase/functions/save-settings/index.ts`
- Crear: `supabase/functions/get-settings/index.ts`

- [ ] **Step 1: Crear supabase/functions/save-settings/index.ts**

  ```ts
  import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
  import { corsHeaders } from '../_shared/cors.ts'
  import { encryptIfPresent } from '../_shared/crypto.ts'

  Deno.serve(async (req) => {
    if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders })

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_ANON_KEY')!,
      { global: { headers: { Authorization: req.headers.get('Authorization')! } } }
    )

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return new Response('Unauthorized', { status: 401, headers: corsHeaders })

    const body = await req.json()

    const row: Record<string, unknown> = {
      user_id: user.id,
      updated_at: new Date().toISOString(),
    }
    if ('groq_api_key'    in body) row.groq_api_key    = await encryptIfPresent(body.groq_api_key)
    if ('google_maps_key' in body) row.google_maps_key = await encryptIfPresent(body.google_maps_key)
    if ('consumption'     in body) row.consumption     = body.consumption
    if ('fuel_price'      in body) row.fuel_price      = body.fuel_price

    const { error } = await supabase.from('user_settings').upsert(row)
    if (error) return new Response(JSON.stringify({ error: error.message }), { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } })

    return new Response(JSON.stringify({ ok: true }), { headers: { ...corsHeaders, 'Content-Type': 'application/json' } })
  })
  ```

- [ ] **Step 2: Crear supabase/functions/get-settings/index.ts**

  ```ts
  import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
  import { corsHeaders } from '../_shared/cors.ts'
  import { decryptIfPresent } from '../_shared/crypto.ts'

  Deno.serve(async (req) => {
    if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders })

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_ANON_KEY')!,
      { global: { headers: { Authorization: req.headers.get('Authorization')! } } }
    )

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return new Response('Unauthorized', { status: 401, headers: corsHeaders })

    const { data, error } = await supabase
      .from('user_settings')
      .select('groq_api_key, google_maps_key, consumption, fuel_price')
      .eq('user_id', user.id)
      .maybeSingle()

    if (error) return new Response(JSON.stringify({ error: error.message }), { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } })
    if (!data)  return new Response(JSON.stringify({}), { headers: { ...corsHeaders, 'Content-Type': 'application/json' } })

    const result = {
      groq_api_key:    await decryptIfPresent(data.groq_api_key),
      google_maps_key: await decryptIfPresent(data.google_maps_key),
      consumption:     data.consumption,
      fuel_price:      data.fuel_price,
    }

    return new Response(JSON.stringify(result), { headers: { ...corsHeaders, 'Content-Type': 'application/json' } })
  })
  ```

- [ ] **Step 3: Desplegar ambas funciones**

  ```bash
  supabase functions deploy save-settings --no-verify-jwt
  supabase functions deploy get-settings  --no-verify-jwt
  ```

  Verificar en dashboard → Edge Functions: deben aparecer `save-settings` y `get-settings`.

- [ ] **Step 4: Commit**

  ```bash
  git add supabase/functions/save-settings supabase/functions/get-settings
  git commit -m "feat: Edge Functions save-settings y get-settings con cifrado AES-256-GCM"
  ```

---

## Task 7: Cargar y guardar settings desde App.jsx

**Files:**
- Modificar: `src/App.jsx`

- [ ] **Step 1: Añadir carga de settings al inicio**

  En `src/App.jsx`, añadir este `useEffect` justo después de los useEffect existentes (del demoStep):

  ```js
  // Cargar settings del usuario desde Supabase al iniciar
  useEffect(() => {
    async function loadSettings() {
      try {
        const { data, error } = await supabase.functions.invoke('get-settings')
        if (error || !data) return
        if (data.groq_api_key)    { setGroqKey(data.groq_api_key);    localStorage.setItem('rt_groqkey', data.groq_api_key) }
        if (data.google_maps_key) { setGoogleKey(data.google_maps_key); localStorage.setItem('rt_googlekey', data.google_maps_key) }
        if (data.consumption != null) { setConsumption(data.consumption); localStorage.setItem('rt_consumption', data.consumption) }
        if (data.fuel_price  != null) { setFuelPrice(data.fuel_price);   localStorage.setItem('rt_fuel_price', data.fuel_price) }
      } catch { /* fallo silencioso — la app funciona con localStorage */ }
    }
    loadSettings()
  }, []) // solo al montar
  ```

- [ ] **Step 2: Añadir guardado en background al cambiar settings**

  Añadir esta función helper después de `saveNum`:

  ```js
  const persistSettings = async (patch) => {
    try {
      await supabase.functions.invoke('save-settings', { body: patch })
    } catch { /* silencioso */ }
  }
  ```

  Modificar `saveKey` para llamar a `persistSettings` en background:

  ```js
  const saveKey = (storageKey, value, setter) => {
    setter(value)
    localStorage.setItem(storageKey, value)
    const field = storageKey === 'rt_groqkey' ? 'groq_api_key' : 'google_maps_key'
    persistSettings({ [field]: value })
  }
  ```

  Modificar `saveNum` para llamar a `persistSettings` en background:

  ```js
  const saveNum = (storageKey, value, setter) => {
    const n = parseFloat(value) || 0
    setter(n)
    localStorage.setItem(storageKey, n)
    const field = storageKey === 'rt_consumption' ? 'consumption' : 'fuel_price'
    persistSettings({ [field]: n })
  }
  ```

- [ ] **Step 3: Verificar**

  - Login con Google → abrir DevTools → Network
  - Buscar llamada a `get-settings` → debe devolver `{}` (primer login, sin settings)
  - Introducir la Groq API Key en el campo → Network debe mostrar llamada a `save-settings` con status 200
  - Cerrar sesión → volver a entrar → el campo de Groq Key debe aparecer ya relleno automáticamente
  - En Supabase dashboard → Table Editor → user_settings: la fila debe tener `groq_api_key` como texto cifrado (no la key real)

- [ ] **Step 4: Commit**

  ```bash
  git add src/App.jsx
  git commit -m "feat: carga y guardado de settings en Supabase al iniciar sesión"
  ```

---

## Task 8: Edge Functions save-route + get-routes

**Files:**
- Crear: `supabase/functions/save-route/index.ts`
- Crear: `supabase/functions/get-routes/index.ts`

- [ ] **Step 1: Crear supabase/functions/save-route/index.ts**

  ```ts
  import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
  import { corsHeaders } from '../_shared/cors.ts'
  import { encryptIfPresent } from '../_shared/crypto.ts'

  Deno.serve(async (req) => {
    if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders })

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_ANON_KEY')!,
      { global: { headers: { Authorization: req.headers.get('Authorization')! } } }
    )

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return new Response('Unauthorized', { status: 401, headers: corsHeaders })

    const body = await req.json()

    // Insertar ruta
    const { data: route, error: routeErr } = await supabase
      .from('routes')
      .insert({
        user_id:        user.id,
        origin:         await encryptIfPresent(body.origin),
        total_km:       body.total_km,
        saving_minutes: body.saving_minutes,
        end_time:       body.end_time,
        worker_count:   body.worker_count ?? 1,
      })
      .select('id')
      .single()

    if (routeErr) return new Response(JSON.stringify({ error: routeErr.message }), { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } })

    // Insertar paradas con PII cifrado
    const stops = await Promise.all(
      (body.stops ?? []).map(async (s: Record<string, unknown>) => ({
        route_id:       route.id,
        position:       s.position,
        client_name:    await encryptIfPresent(s.client_name as string),
        client_address: await encryptIfPresent(s.client_address as string),
        client_phone:   await encryptIfPresent(s.client_phone as string),
        duration_min:   s.duration_min,
        window_type:    s.window_type,
        window_start:   s.window_start,
        window_end:     s.window_end,
        arrival_time:   s.arrival_time,
        travel_minutes: s.travel_minutes,
      }))
    )

    const { error: stopsErr } = await supabase.from('route_stops').insert(stops)
    if (stopsErr) return new Response(JSON.stringify({ error: stopsErr.message }), { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } })

    return new Response(JSON.stringify({ route_id: route.id }), { headers: { ...corsHeaders, 'Content-Type': 'application/json' } })
  })
  ```

- [ ] **Step 2: Crear supabase/functions/get-routes/index.ts**

  ```ts
  import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
  import { corsHeaders } from '../_shared/cors.ts'
  import { decryptIfPresent } from '../_shared/crypto.ts'

  Deno.serve(async (req) => {
    if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders })

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_ANON_KEY')!,
      { global: { headers: { Authorization: req.headers.get('Authorization')! } } }
    )

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return new Response('Unauthorized', { status: 401, headers: corsHeaders })

    const { data: routes, error } = await supabase
      .from('routes')
      .select(`
        id, created_at, origin, total_km, saving_minutes, end_time, worker_count,
        route_stops (
          position, client_name, client_address, client_phone,
          duration_min, window_type, window_start, window_end,
          arrival_time, travel_minutes
        )
      `)
      .eq('user_id', user.id)
      .order('created_at', { ascending: false })
      .limit(50)

    if (error) return new Response(JSON.stringify({ error: error.message }), { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } })

    // Descifrar PII en cada ruta y parada
    const decrypted = await Promise.all((routes ?? []).map(async (route) => ({
      ...route,
      origin: await decryptIfPresent(route.origin),
      route_stops: await Promise.all((route.route_stops ?? []).map(async (s) => ({
        ...s,
        client_name:    await decryptIfPresent(s.client_name),
        client_address: await decryptIfPresent(s.client_address),
        client_phone:   await decryptIfPresent(s.client_phone),
      }))),
    })))

    return new Response(JSON.stringify(decrypted), { headers: { ...corsHeaders, 'Content-Type': 'application/json' } })
  })
  ```

- [ ] **Step 3: Desplegar**

  ```bash
  supabase functions deploy save-route  --no-verify-jwt
  supabase functions deploy get-routes  --no-verify-jwt
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add supabase/functions/save-route supabase/functions/get-routes
  git commit -m "feat: Edge Functions save-route y get-routes con cifrado PII"
  ```

---

## Task 9: Auto-guardar ruta tras optimización en App.jsx

**Files:**
- Modificar: `src/App.jsx`

- [ ] **Step 1: Añadir useEffect que guarda la ruta cuando result cambia**

  En `src/App.jsx`, añadir después del useEffect de demoStep:

  ```js
  // Auto-guardar ruta en Supabase tras cada optimización (no durante demo)
  useEffect(() => {
    if (!result || demoStep !== null) return
    if (!stopCoords.length) return

    const payload = {
      origin,
      total_km:       result.total_km ?? result.savings_breakdown?.optimised_km ?? 0,
      saving_minutes: result.saving_minutes ?? 0,
      end_time:       result.end_time ?? '',
      worker_count:   workers.length || 1,
      stops: stopCoords.map(({ stop, order }) => ({
        position:       stop.position,
        client_name:    order.cliente    ?? '',
        client_address: order.direccion  ?? '',
        client_phone:   order.telefono   ?? '',
        duration_min:   order.duracion   ?? 0,
        window_type:    order.ventana_tipo    ?? 'flexible',
        window_start:   order.ventana_inicio  ?? '',
        window_end:     order.ventana_fin     ?? '',
        arrival_time:   stop.arrival_time     ?? '',
        travel_minutes: stop.travel_minutes   ?? 0,
      })),
    }

    supabase.functions.invoke('save-route', { body: payload }).catch(() => {})
  }, [result]) // eslint-disable-line react-hooks/exhaustive-deps
  ```

- [ ] **Step 2: Verificar**

  - Optimizar una ruta real (con datos cargados vía CSV)
  - En Supabase dashboard → Table Editor → `routes`: debe aparecer una nueva fila
  - En `route_stops`: filas con `client_name`, `client_address`, `client_phone` como texto cifrado (no legible)
  - En la web (paso siguiente) el historial mostrará los datos descifrados en claro

- [ ] **Step 3: Commit**

  ```bash
  git add src/App.jsx
  git commit -m "feat: auto-guardar ruta en Supabase tras optimización"
  ```

---

## Task 10: HistoryPage completo

**Files:**
- Modificar: `src/pages/HistoryPage.jsx` (reemplazar el stub del Task 3)
- Modificar: `src/index.css` (añadir estilos de historial)

- [ ] **Step 1: Implementar src/pages/HistoryPage.jsx**

  ```jsx
  import { useState, useEffect } from 'react'
  import { useNavigate } from 'react-router-dom'
  import { ArrowLeft, ChevronDown, ChevronUp, LogOut, Clock, MapPin, Phone } from 'lucide-react'
  import { supabase } from '../utils/supabaseClient'

  export default function HistoryPage() {
    const [routes,   setRoutes]   = useState([])
    const [loading,  setLoading]  = useState(true)
    const [expanded, setExpanded] = useState(null)
    const navigate = useNavigate()

    useEffect(() => {
      supabase.functions.invoke('get-routes').then(({ data }) => {
        setRoutes(data ?? [])
        setLoading(false)
      })
    }, [])

    const handleLogout = async () => {
      await supabase.auth.signOut()
      navigate('/login')
    }

    const formatDate = (iso) => {
      const d = new Date(iso)
      return d.toLocaleDateString('es-ES', { day: '2-digit', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' })
    }

    return (
      <div className="history-page">
        <div className="history-header">
          <button className="history-back-btn" onClick={() => navigate('/')}>
            <ArrowLeft size={14} strokeWidth={2} /> Volver a la app
          </button>
          <div className="history-title">Historial de rutas</div>
          <button className="btn-logout" onClick={handleLogout}>Salir</button>
        </div>

        <div className="history-content">
          {loading && <div className="history-empty">Cargando…</div>}
          {!loading && routes.length === 0 && (
            <div className="history-empty">Aún no tienes rutas guardadas. Optimiza una ruta para verla aquí.</div>
          )}
          {routes.map(route => (
            <div key={route.id} className="history-route-card">
              <div className="history-route-header" onClick={() => setExpanded(expanded === route.id ? null : route.id)}>
                <div className="history-route-meta">
                  <span className="history-route-date">{formatDate(route.created_at)}</span>
                  <div className="history-route-stats">
                    <span>{route.route_stops?.length ?? 0} paradas</span>
                    <span>{route.total_km} km</span>
                    <span>{route.saving_minutes} min ahorrados</span>
                    {route.worker_count > 1 && <span>{route.worker_count} técnicos</span>}
                  </div>
                </div>
                {expanded === route.id ? <ChevronUp size={14} /> : <ChevronDown size={14} />}
              </div>

              {expanded === route.id && (
                <div className="history-stops">
                  {(route.route_stops ?? [])
                    .sort((a, b) => a.position - b.position)
                    .map(stop => (
                      <div key={stop.position} className="history-stop-row">
                        <div className="history-stop-pos">{stop.position}</div>
                        <div className="history-stop-body">
                          <div className="history-stop-name">{stop.client_name}</div>
                          <div className="history-stop-detail">
                            <MapPin size={10} strokeWidth={1.5} /> {stop.client_address}
                          </div>
                          {stop.client_phone && (
                            <div className="history-stop-detail">
                              <Phone size={10} strokeWidth={1.5} /> {stop.client_phone}
                            </div>
                          )}
                        </div>
                        <div className="history-stop-time">
                          {stop.arrival_time && <div>{stop.arrival_time}</div>}
                          {stop.window_end && <div className="history-stop-window">→ {stop.window_end}</div>}
                          <div className="history-stop-tag" data-type={stop.window_type}>
                            {stop.window_type === 'fija' ? 'FIJA' : 'FLEX'}
                          </div>
                        </div>
                      </div>
                    ))}
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    )
  }
  ```

- [ ] **Step 2: Añadir CSS de historial en src/index.css**

  ```css
  /* ── History page ── */
  .history-page {
    min-height: 100vh;
    background: var(--bg);
    display: flex;
    flex-direction: column;
  }
  .history-header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 24px;
    background: white;
    border-bottom: 1px solid var(--border);
  }
  .history-back-btn {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    color: var(--muted2);
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px 0;
  }
  .history-back-btn:hover { color: var(--text); }
  .history-title {
    flex: 1;
    font-size: 14px;
    font-weight: 600;
    color: var(--text);
    text-align: center;
  }
  .history-content {
    max-width: 720px;
    margin: 24px auto;
    width: 100%;
    padding: 0 16px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .history-empty {
    text-align: center;
    color: var(--muted2);
    font-size: 13px;
    padding: 48px 0;
  }
  .history-route-card {
    background: white;
    border: 1px solid var(--border);
    border-radius: 8px;
    overflow: hidden;
  }
  .history-route-header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    cursor: pointer;
  }
  .history-route-header:hover { background: var(--bg); }
  .history-route-meta { flex: 1; }
  .history-route-date {
    font-size: 12px;
    font-weight: 600;
    color: var(--text);
    display: block;
    margin-bottom: 4px;
  }
  .history-route-stats {
    display: flex;
    gap: 12px;
    font-size: 11px;
    color: var(--muted2);
  }
  .history-stops {
    border-top: 1px solid var(--border);
    padding: 8px 0;
  }
  .history-stop-row {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 8px 16px;
    border-bottom: 1px solid var(--bg);
  }
  .history-stop-row:last-child { border-bottom: none; }
  .history-stop-pos {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: var(--text);
    color: white;
    font-size: 10px;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 1px;
  }
  .history-stop-body { flex: 1; }
  .history-stop-name {
    font-size: 12px;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 3px;
  }
  .history-stop-detail {
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 11px;
    color: var(--muted2);
    margin-bottom: 2px;
  }
  .history-stop-time {
    text-align: right;
    font-size: 11px;
    color: var(--text);
    min-width: 60px;
  }
  .history-stop-window { color: var(--muted2); font-size: 10px; }
  .history-stop-tag {
    font-size: 9px;
    font-weight: 600;
    letter-spacing: 0.5px;
    padding: 1px 5px;
    border-radius: 3px;
    margin-top: 3px;
    display: inline-block;
  }
  .history-stop-tag[data-type="fija"]     { color: #b45309; border: 1px solid #f0c060; }
  .history-stop-tag[data-type="flexible"] { color: #0e7490; border: 1px solid #7dd3e0; }
  ```

- [ ] **Step 3: Añadir enlace al historial en App.jsx**

  En `src/App.jsx`, localizar el botón "Salir" en el header y añadir antes el enlace al historial:

  ```jsx
  import { useNavigate } from 'react-router-dom'
  // (ya importado en Task 4)

  // En el JSX del header, antes del btn-logout:
  <button className="btn-history" onClick={() => navigate('/historial')} title="Ver historial">
    Historial
  </button>
  ```

  Añadir CSS en `src/index.css`:

  ```css
  .btn-history {
    font-size: 11px;
    color: var(--muted2);
    background: none;
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 4px 10px;
    cursor: pointer;
    transition: color 0.15s;
  }
  .btn-history:hover { color: var(--text); }
  ```

- [ ] **Step 4: Verificar**

  - Optimizar una ruta → hacer clic en "Historial" → aparece la ruta guardada
  - Hacer clic en la fila → se expande mostrando cliente, dirección y teléfono en claro
  - En Supabase Table Editor → `route_stops`: los mismos campos aparecen cifrados
  - Hacer clic en "Volver a la app" → regresa a la app principal

- [ ] **Step 5: Commit**

  ```bash
  git add src/pages/HistoryPage.jsx src/App.jsx src/index.css
  git commit -m "feat: HistoryPage con lista y detalle de rutas guardadas"
  ```

---

## Task 11: Build final + push

**Files:** ninguno nuevo

- [ ] **Step 1: Build de producción**

  ```bash
  npm run build
  ```

  Debe completarse sin errores. Output esperado:
  ```
  ✓ built in ~10s
  dist/index.html
  dist/assets/index-*.css
  dist/assets/index-*.js
  ```

- [ ] **Step 2: Smoke test en preview**

  ```bash
  npm run preview
  ```

  - Abrir http://localhost:4173 → redirige a `/login`
  - Login con Google → llega a la app
  - API keys auto-cargadas si ya se habían guardado
  - Navegar a `/historial` → muestra rutas previas

- [ ] **Step 3: Push**

  ```bash
  git push origin feature/route-enhancements:master
  ```

- [ ] **Step 4: (Opcional) Actualizar Site URL en Supabase**

  Si el proyecto está desplegado en producción (Vercel, Netlify, etc.):
  - Supabase dashboard → Authentication → URL Configuration
  - Site URL: URL de producción
  - Redirect URLs: añadir URL de producción
  - Google Cloud Console → OAuth Client → añadir URL de producción a Authorized redirect URIs:
    `https://TU_PROJECT_REF.supabase.co/auth/v1/callback` (ya está) +
    el dominio de producción para el redirect final

---

## Resumen de variables de entorno

| Variable | Dónde se configura | Valor |
|---|---|---|
| `VITE_SUPABASE_URL` | `.env` (frontend) | `https://xxx.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `.env` (frontend) | `eyJ...` (anon key pública) |
| `ENCRYPTION_KEY` | `supabase secrets set` | 64 chars hex (generado con node crypto) |
